<view class="friendBox">
	<scroll-view scroll-y style="height:{{height}}px;" bindscrolltolower="loadMore">
	<view class="itemList">
		<view class="item" wx:for="{{friendListData}}" wx:for-index="index" wx:for-item="item" data-id="{{item.id}}" bindtap="viewFriendDetail">
			<image src="{{item.logo}}" class="icon"></image>
			<image src="../../dist/images/path.png" class="arrow"></image>
			<view class="name">{{item.sourceName}}</view>
		</view>
	</view>
	</scroll-view>
</view>
